{
  "name": "gcr-front-end-data",
  "version": "1.0.0",
  "description": "GCR front end data API — JSON-backed, no database",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "sync": "node sync.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0"
  }
}
